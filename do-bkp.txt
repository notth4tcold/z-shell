ALWAYS DO BKP OF:

.zsh_history
.ssh/
.gitconfig

ENCRYPT:
tar -czf backup_configs.tar.gz ~/.ssh ~/.zsh_history ~/.gitconfig
gpg -c backup_configs.tar.gz

DECRYPT:
gpg -d backup.gpg > backup_configs.tar.gz
tar -xzf backup_configs.tar.gz
